version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: nhakhoa-postgres
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres123}
      POSTGRES_DB: ${DB_NAME:-nhakhoa_db}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8"
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db:/docker-entrypoint-initdb.d
    # networks:
    #   - nhakhoa-network
    # # healthcheck:
    # #   test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
    # #   interval: 10s
    # #   timeout: 5s
    # #   retries: 5

  minio:
    image: minio/minio:latest
    container_name: nhakhoa-minio
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin123}
    ports:
      - "9000:9000"     # MinIO API
      - "9001:9001"     # MinIO Console
    volumes:
      - minio_data:/minio/data
    # networks:
    #   - nhakhoa-network
    # command: minio server /minio/data --console-address ":9001"
    # healthcheck:
    #   test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
    #   interval: 30s
    #   timeout: 20s
    #   retries: 3

  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   container_name: nhakhoa-backend
  #   environment:
  #     NODE_ENV: ${NODE_ENV:-development}
  #     DB_HOST: postgres
  #     DB_PORT: 5432
  #     DB_USER: ${DB_USER:-postgres}
  #     DB_PASSWORD: ${DB_PASSWORD:-postgres123}
  #     DB_NAME: ${DB_NAME:-nhakhoa_db}
  #     MINIO_HOST: minio
  #     MINIO_PORT: 9000
  #     MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
  #     MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin123}
  #     MINIO_BUCKET: ${MINIO_BUCKET:-nhakhoa}
  #     MINIO_USE_SSL: ${MINIO_USE_SSL:-false}
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     minio:
  #       condition: service_healthy
  #   volumes:
  #     # Mount code để dev hotreload
  #     - ./backend/src:/app/src
  #     - ./backend/node_modules:/app/node_modules
  #   networks:
  #     - nhakhoa-network
  #   command: npm run dev

volumes:
  postgres_data:
    driver: local
  minio_data:
    driver: local

# networks:
#   nhakhoa-network:
#     driver: bridge
